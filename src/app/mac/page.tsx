import Resizable from '@/components/Product/Resizable';
import { FunctionComponent } from 'react';

interface MacProps {}

const Mac: FunctionComponent<MacProps> = () => {
    return (
        <div>
            <Resizable />
        </div>
    );
};

export default Mac;
